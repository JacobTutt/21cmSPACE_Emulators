[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "21cmspace-emulators"
version = "0.1.0"
description = "JAX-native emulators for 21cmSPACE 21-cm inference workflows."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jax",
  "flax",
  "orbax-checkpoint",
  "optax",
  "numpy",
  "scipy",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[project.scripts]
21cmspace-delta21-train = "jaxemu_21cmSPACE.emulators21.delta21.train:main"
21cmspace-delta21-infer = "jaxemu_21cmSPACE.emulators21.delta21.infer:main"
21cmspace-t21-train = "jaxemu_21cmSPACE.emulators21.t21.train:main"
21cmspace-t21-infer = "jaxemu_21cmSPACE.emulators21.t21.infer:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["jaxemu_21cmSPACE*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
