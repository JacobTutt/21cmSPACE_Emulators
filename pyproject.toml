[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nenufar-emulators"
version = "0.1.0"
description = "JAX-native emulators for NenuFAR-era 21-cm inference workflows."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jax",
  "flax",
  "optax",
  "numpy",
  "scipy",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[project.scripts]
nenufar-power-train = "nenufar_emulators.power_spectrum.train:main"
nenufar-power-infer = "nenufar_emulators.power_spectrum.infer:main"
nenufar-global-train = "nenufar_emulators.global_signal.train:main"
nenufar-global-infer = "nenufar_emulators.global_signal.infer:main"

[tool.setuptools]
package-dir = {"" = "src_jax"}

[tool.setuptools.packages.find]
where = ["src_jax"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src_jax"]
